diff --git a/client/src/App.jsx b/client/src/App.jsx
index d366758..810bf03 100644
--- a/client/src/App.jsx
+++ b/client/src/App.jsx
@@ -22,8 +22,15 @@ import VisualIdentityCard from "./components/VisualIdentityCard"; // <--- V8.0 V
 import VisualBodyMap from "./components/VisualBodyMap"; // <--- V9.6 Visual Body Map
 import useCortex from "./hooks/useCortex"; // <--- T.I.L.O Logical Engine
 import SearchableVerticalMenu from "./components/ui/SearchableVerticalMenu"; // V8.0 Searchable Vertical Menu
+import Fase3_MotivoConsulta from "./components/interview/Fase3_MotivoConsulta";
 import Fase4_AntecedentesFamiliares from "./components/interview/Fase4_AntecedentesFamiliares";
 import Fase5_EstiloVida from "./components/interview/Fase5_EstiloVida";
+import Fase12_Biometria from "./components/interview/Fase12_Biometria";
+import Fase13_ContextoEspecial from "./components/interview/Fase13_ContextoEspecial";
+import Fase14_Orquestador from "./components/interview/Fase14_Orquestador";
+import Fase15_SuplementacionAv from "./components/interview/Fase15_SuplementacionAv";
+import Fase16_ProtocoloDietetico from "./components/interview/Fase16_ProtocoloDietetico";
+import Fase17_Despedida from "./components/interview/Fase17_Despedida";
 
 // 1. Funci├│n Auxiliar (Fuera del componente)
 // 2. Calculadora de Edad Exacta
@@ -486,7 +493,8 @@ function App() {
     clearSession,
     setCurrentPhase,
     apiContext,
-    fase3State
+    fase3State,
+    setFase3State
   } = useCortex();
 
   const [input, setInput] = useState("");
@@ -682,8 +690,8 @@ function App() {
 
     // If it was handing off from Phase 2 (Emergency):
     if (currentPhase === 'PHASE_2_COMPLETE_HANDOFF' && interviewStep === 'appointment') {
-      console.log("­ƒöä Cortex Handoff Phase 2 -> Cortex Phase 3");
-      setCurrentPhase('PHASE_3_OPEN_PROMPT');
+      console.log("­ƒöä Cortex Handoff Phase 2 -> Legacy Phase 3 Component");
+      setCurrentPhase('PHASE_3_MOTIVO_CONSULTA');
     }
     // Handoff to Legacy App.jsx Phase 10 (Habits) from Cortex:
     // If handing off from Phase 9 (Physiological) to Phase 10:
@@ -714,12 +722,18 @@ function App() {
     }
 
     if (currentPhase === 'PHASE_13_COMPLETE_HANDOFF' && interviewStep === 'appointment') {
-      console.log("­ƒöä Cortex Handoff Phase 13 (Biometrics) -> Legacy App.jsx Finished");
+      console.log("­ƒöä Cortex Handoff Phase 13 (Biometrics) -> Fase14_Orquestador");
+      setCurrentPhase('PHASE_14_ORQUESTADOR');
+      saveSessionProgress(13, 'appointment', false);
+    }
+
+    if (currentPhase === 'PHASE_17_DASHBOARD_RENDER' && interviewStep === 'appointment') {
+      console.log("­ƒöä Cortex Handoff Phase 17 -> Legacy App.jsx Finished");
       setInterviewStep('finished');
       setActiveTab('diagnosis');
-      saveSessionProgress(13, 'finished', false);
+      saveSessionProgress(17, 'finished', false);
     }
-  }, [currentPhase, interviewStep, patientData.habits?.alcohol, patientData.identificacion?.sexo, saveSessionProgress, setActiveTab, setCurrentPhase, setMessages, showPrivacyPolicy, isIdentityConfirmed]);
+  }, [currentPhase, interviewStep, patientData.habits?.alcohol, patientData.identificacion.sexo, saveSessionProgress, setActiveTab, setCurrentPhase, setMessages, showPrivacyPolicy, isIdentityConfirmed]);
 
   // --- MOTOR DE SEGURIDAD CULTURAL (OBSERVER) ---
   // Detecta cambios en religi├│n o IMC y aplica "Hard Constraints"
@@ -742,7 +756,7 @@ function App() {
       });
     }
     // eslint-disable-next-line react-hooks/exhaustive-deps
-  }, [patientData.identificacion?.religion, patientData.imc]);
+  }, [patientData.identificacion.religion, patientData.imc]);
 
 
 
@@ -4819,12 +4833,8 @@ Para descartar condiciones que requieran atenci├│n especial, ┬┐ha notado recient
         {isLoggedIn && !showPrivacyPolicy && (
           <Header
             user={user}
-            sessionInfo={{
-              citation: apiContext?.citaId || apiContext?.idCita || sessionMetadata.citation,
-              userId: apiContext?.userId || sessionMetadata.userId || '---',
-              patientName: apiContext?.rawName || sessionMetadata.patientName || sessionMetadata.serverName || '---'
-            }}
-            showSessionInfo={true} // <--- AHORA SIEMPRE SE MUESTRA SI HAY DATOS
+            sessionInfo={apiContext?.citaId ? { citation: apiContext.citaId, userId: apiContext.userId, patientName: apiContext.rawName } : sessionMetadata}
+            showSessionInfo={isIdentityConfirmed} // <--- AHORA DEPENDE DE LA CONFIRMACI├ôN DE "SOY YO"
             clearSession={clearSession}
             activeTab={activeTab} // UI/UX #4: Navegaci├│n Global (Desactivada en Fase 0)
             onTabChange={setActiveTab}    // UI/UX #4: Navegaci├│n Global
@@ -4842,23 +4852,18 @@ Para descartar condiciones que requieran atenci├│n especial, ┬┐ha notado recient
           {/* --- COLUMNA IZQUIERDA: CHAT --- */}
           <div className="w-1/2 flex flex-col border-r border-slate-200 bg-white shadow-xl z-10 relative">
 
-            {interviewStep === 'finished' ? (
-              <div className="flex-1 h-full flex items-center justify-center p-8 bg-slate-50 relative">
-                <div className="bg-white p-8 rounded-2xl shadow-lg border border-slate-100 max-w-sm text-center flex flex-col items-center gap-6">
-                  <motion.div
-                    initial={{ scale: 0 }}
-                    animate={{ scale: 1 }}
-                    className="w-20 h-20 rounded-full bg-slate-100 flex-shrink-0 border shadow-md flex items-center justify-center overflow-hidden mb-2 mx-auto"
-                  >
-                    <img src={tiloImg} alt="Tilo" className="w-16 h-16 object-contain" />
-                  </motion.div>
-                  <h2 className="text-xl font-bold text-slate-800">Entrevista Finalizada</h2>
-                  <p className="text-slate-600 leading-relaxed text-sm">
-                    Expediente cerrado y guardado. Cediendo el control total al Bio-Arquitecto para la explicaci├│n final del plan al paciente.
-                  </p>
-                  <div className="w-16 h-1 bg-blue-500 rounded-full mt-2 opacity-50 mx-auto"></div>
-                </div>
-              </div>
+            {interviewStep === 'finished' || currentPhase === 'PHASE_17_DASHBOARD_RENDER' ? (
+              <Fase17_Despedida patientData={patientData} />
+            ) : currentPhase === 'PHASE_3_MOTIVO_CONSULTA' ? (
+              <Fase3_MotivoConsulta
+                setMessages={setMessages}
+                patientData={patientData}
+                setPatientData={setPatientData}
+                onPhaseComplete={(phase3Data) => {
+                  setFase3State(prev => ({ ...prev, ...phase3Data }));
+                  setCurrentPhase('PHASE_4_FAMILY_HISTORY'); // Transition to Phase 4
+                }}
+              />
             ) : currentPhase === 'PHASE_4_FAMILY_HISTORY' ? (
               <Fase4_AntecedentesFamiliares
                 user={user}
@@ -4880,7 +4885,51 @@ Para descartar condiciones que requieran atenci├│n especial, ┬┐ha notado recient
                 initialChatHistory={messages}
                 onPhaseComplete={(lifestyleData) => {
                   setPatientData(prev => ({ ...prev, lifeStyleInfo: lifestyleData }));
-                  setCurrentPhase('PHASE_6_SOMETHING_ELSE'); // Placeholder for next phase
+                  setCurrentPhase('PHASE_12_BIOMETRIA'); // Temporary wire up to Phase 12 to test Fases finales
+                }}
+              />
+            ) : currentPhase === 'PHASE_12_BIOMETRIA' ? (
+              <Fase12_Biometria
+                patientData={patientData}
+                setPatientData={setPatientData}
+                isYouth={patientData?.profile?.age < 18}
+                onPhaseComplete={(nextPhase) => {
+                  setCurrentPhase(nextPhase);
+                }}
+              />
+            ) : currentPhase === 'PHASE_13_SPECIAL_CONTEXT' ? (
+              <Fase13_ContextoEspecial
+                setPatientData={setPatientData}
+                isYouth={patientData?.profile?.age < 18}
+                onPhaseComplete={(msgs, nextPhase) => {
+                  if (nextPhase === 'PHASE_14_ORCHESTRATION_START') {
+                    setCurrentPhase('PHASE_14_ORQUESTADOR');
+                  } else {
+                    setCurrentPhase(nextPhase);
+                  }
+                }}
+              />
+            ) : currentPhase === 'PHASE_14_ORQUESTADOR' ? (
+              <Fase14_Orquestador
+                patientData={patientData}
+                onPhaseComplete={(nextPhase) => {
+                  setCurrentPhase(nextPhase);
+                }}
+              />
+            ) : currentPhase === 'PHASE_15_SUPPLEMENTATION' ? (
+              <Fase15_SuplementacionAv
+                patientData={patientData}
+                setPatientData={setPatientData}
+                onPhaseComplete={(nextPhase) => {
+                  setCurrentPhase(nextPhase);
+                }}
+              />
+            ) : currentPhase === 'PHASE_16_DIETARY_PROTOCOL' ? (
+              <Fase16_ProtocoloDietetico
+                patientData={patientData}
+                setPatientData={setPatientData}
+                onPhaseComplete={(nextPhase) => {
+                  setCurrentPhase(nextPhase);
                 }}
               />
             ) : (
@@ -5126,24 +5175,9 @@ Para descartar condiciones que requieran atenci├│n especial, ┬┐ha notado recient
                           </div>
                         ) : (
                           <input
-                            type={messages.length > 0 && messages[messages.length - 1].inputType === 'tel' ? 'tel' : 'text'}
+                            type="text"
                             value={input}
-                            onChange={(e) => {
-                              if (messages.length > 0 && messages[messages.length - 1].inputType === 'tel') {
-                                let val = e.target.value.replace(/\D/g, '');
-                                if (val.length > 10) val = val.slice(0, 10);
-                                if (val.length > 6) {
-                                  val = `(${val.slice(0,3)}) ${val.slice(3,6)}-${val.slice(6)}`;
-                                } else if (val.length > 3) {
-                                  val = `(${val.slice(0,3)}) ${val.slice(3)}`;
-                                } else if (val.length > 0) {
-                                  val = `(${val}`;
-                                }
-                                setInput(val);
-                              } else {
-                                setInput(e.target.value);
-                              }
-                            }}
+                            onChange={(e) => setInput(e.target.value)}
                             onKeyDown={(e) => e.key === "Enter" && handleSend()}
                             placeholder="Escribe tu respuesta..."
                             className="flex-1 bg-transparent outline-none text-slate-700 placeholder:text-slate-400 text-sm h-10 px-2"
diff --git a/client/src/components/MedicalDashboard.jsx b/client/src/components/MedicalDashboard.jsx
index e3d6ef4..9549db9 100644
--- a/client/src/components/MedicalDashboard.jsx
+++ b/client/src/components/MedicalDashboard.jsx
@@ -228,20 +228,19 @@ export const MedicalDashboard = ({
             targetChild = 'childIdentity';
             targetCardId = 'card-intro';
         }
-        else if (isStepIn(['phase_3_', 'phase_4_'])) {
+        else if (isStepIn(['phase_3_'])) {
             targetParent = 'parentClinical';
             targetChild = 'childMotive';
             targetCardId = 'card-motivo';
         }
-        else if (isStepIn(['ipas_', 'motivo', 'sympt', 'triage', 'ph4_'])) {
+        else if (isStepIn(['ipas_', 'motivo', 'sympt', 'triage'])) {
             targetParent = 'parentClinical';
             targetChild = 'childMotive';
             targetCardId = 'card-motivo';
         }
-        // 2. HISTORIA CL├ìNICA
         else if (isStepIn(stepsClinical)) {
             targetParent = 'parentClinical';
-            if (step.includes('ahf_') || step.includes('family')) { targetChild = 'childAhf'; targetCardId = 'card-ahf'; }
+            if (step.includes('ahf_') || step.includes('family') || step.includes('phase_4_') || step.includes('ph4_')) { targetChild = 'childAhf'; targetCardId = 'card-ahf'; }
             else if (step.includes('app_') || step.includes('patho')) { targetChild = 'childApp'; targetCardId = 'card-app'; }
             else if (step.includes('meds_') || step.includes('supp_')) { targetChild = 'childFarma'; targetCardId = 'card-meds'; }
             else if (step.includes('allergies_')) { targetChild = 'childAllergies'; targetCardId = 'card-allergy'; }
@@ -255,7 +254,7 @@ export const MedicalDashboard = ({
             else { targetChild = 'childAhf'; targetCardId = 'card-ahf'; } // Fallback for general clinical
         }
         // 3. ESTILO DE VIDA Y ENTORNO
-        else if (isStepIn(stepsLifestyle)) {
+        else if (isStepIn(stepsLifestyle) || step.includes('phase_5_')) {
             targetParent = 'parentLifestyle';
             targetChild = null; // No child accordion in TabLogistics
             targetCardId = 'card-lifestyle';
@@ -287,7 +286,7 @@ export const MedicalDashboard = ({
                         [targetParent]: true,
                         ...(targetChild ? { [targetChild]: true } : {}),
                         // Fix for Phase 4: Keep Motivo de Consulta open alongside AHF
-                        ...(step.includes('ahf_') || step.includes('PHASE_4_') ? { childAhf: true } : {})
+                        ...(step.includes('ahf_') || step.includes('phase_4_') || step.includes('ph4_') ? { childAhf: true } : {})
                     };
                 });
             }, 0);
diff --git a/client/src/components/interview/Fase0_Identidad.jsx b/client/src/components/interview/Fase0_Identidad.jsx
index 82fe728..6ce0378 100644
--- a/client/src/components/interview/Fase0_Identidad.jsx
+++ b/client/src/components/interview/Fase0_Identidad.jsx
@@ -23,18 +23,21 @@ export const Fase0_Identidad = ({
     const identityLock = useClinicalGenome(state => state.identityLock);
     const updateIdentityLock = useClinicalGenome(state => state.updateIdentityLock);
 
-    const handleSend = (text) => {
-        if (!text.trim()) return;
+    const handleSend = (text, directValue = null) => {
+        const rawMsg = directValue !== null ? directValue : text;
+        if (!rawMsg.trim()) return;
 
-        const newMessages = [...messages, { role: 'user', content: text }];
+        const displayMsg = rawMsg;
+        const newMessages = [...messages, { role: 'user', content: displayMsg }];
         setMessages(newMessages);
         setInputValue("");
         setIsAnalyzing(true);
 
         setTimeout(() => {
-            const cleanText = formatText(text.trim());
+            const cleanText = formatText(rawMsg.trim());
             let nextStep = step;
             let responseMsg = "";
+            let options = null;
 
             switch (step) {
                 case 'intro_name':
@@ -50,8 +53,27 @@ export const Fase0_Identidad = ({
                     break;
                 case 'intro_phone': {
                     updateIdentityLock({ patientInfo: { ...identityLock.patientInfo, phone: cleanText } });
-                    responseMsg = "Queda registrado.\n\nPerfil de identidad completado transitoriamente. Transferiendo control...";
-                    nextStep = 'completed';
+                    const pInfo = { ...identityLock.patientInfo, phone: cleanText };
+                    responseMsg = `A continuaci├│n, le presento un resumen de los datos capturados:\n\n` +
+                                  `**Nombre Completo:** ${pInfo.name}\n` +
+                                  `**CURP:** ${pInfo.curp}\n` +
+                                  `**Tel├®fono Celular:** ${pInfo.phone}\n\n` +
+                                  `┬┐Son correctos estos datos?`;
+                    options = [
+                        { label: "Ô£à S├ì, SON CORRECTOS", value: "CONFIRM_DATA" },
+                        { label: "ÔØî NO, CORREGIR DATOS", value: "CORRECT_DATA" }
+                    ];
+                    nextStep = 'intro_review';
+                    break;
+                }
+                case 'intro_review': {
+                    if (directValue === 'CONFIRM_DATA') {
+                        responseMsg = "Perfil de identidad completado transitoriamente. Transfiriendo control...";
+                        nextStep = 'completed';
+                    } else {
+                        responseMsg = "Entendido, vamos a corregir los datos.\n\n┬┐Podr├¡a proporcionarme su **Nombre Completo** (comenzando por nombres) nuevamente?";
+                        nextStep = 'intro_name';
+                    }
                     break;
                 }
                 default:
@@ -59,7 +81,7 @@ export const Fase0_Identidad = ({
                     break;
             }
 
-            setMessages(prev => [...prev, { role: 'assistant', content: responseMsg }]);
+            setMessages(prev => [...prev, { role: 'assistant', content: responseMsg, options }]);
             setStep(nextStep);
             setIsAnalyzing(false);
 
@@ -73,6 +95,20 @@ export const Fase0_Identidad = ({
 
     useEffect(() => { chatEndRef.current?.scrollIntoView({ behavior: 'smooth' }); }, [messages]);
 
+    const handlePhoneChange = (e) => {
+        let val = e.target.value.replace(/\D/g, '');
+        if (val.length > 10) val = val.slice(0, 10);
+        let formatted = val;
+        if (val.length > 6) {
+            formatted = `(${val.slice(0, 3)}) ${val.slice(3, 6)}-${val.slice(6)}`;
+        } else if (val.length > 3) {
+            formatted = `(${val.slice(0, 3)}) ${val.slice(3)}`;
+        } else if (val.length > 0) {
+            formatted = `(${val}`;
+        }
+        setInputValue(formatted);
+    };
+
     return (
         <div className="flex h-full w-full bg-[#F8FAFC] overflow-hidden font-sans">
             <div className="w-full flex flex-col border-r border-slate-200 bg-white">
@@ -84,6 +120,22 @@ export const Fase0_Identidad = ({
                             </div>
                             <div className={`p-4 rounded-2xl text-sm shadow-sm leading-relaxed max-w-[80%] whitespace-pre-line ${msg.role === 'user' ? 'bg-[#1C75BC] text-white rounded-tr-none' : 'bg-white border border-slate-200 rounded-tl-none text-slate-700'}`}>
                                 {msg.content}
+                                
+                                {/* Render Options if any (Only for the latest message from assistant) */}
+                                {msg.options && msg.role === 'assistant' && idx === messages.length - 1 && msg.options.length > 0 && (
+                                    <div className="mt-4 flex flex-col gap-2">
+                                        {msg.options.map((opt, oIdx) => (
+                                            <button
+                                                key={oIdx}
+                                                onClick={() => handleSend(opt.label, opt.value)}
+                                                className="w-full text-left px-4 py-3 rounded-lg border border-[#1C75BC] text-[#1C75BC] hover:bg-[#1C75BC] hover:text-white transition-all duration-200 font-medium bg-white"
+                                                disabled={isAnalyzing || step === 'completed'}
+                                            >
+                                                {opt.label}
+                                            </button>
+                                        ))}
+                                    </div>
+                                )}
                             </div>
                         </div>
                     ))}
@@ -99,9 +151,10 @@ export const Fase0_Identidad = ({
                 <div className="p-6 bg-white border-t border-slate-100">
                     <div className="flex items-center gap-3 bg-slate-50 p-2 rounded-full border border-slate-200 focus-within:ring-2 focus-within:ring-blue-500 focus-within:bg-white transition-all shadow-inner">
                         <input
-                            type="text" value={inputValue} onChange={(e) => setInputValue(e.target.value)}
+                            type={step === 'intro_phone' ? 'tel' : 'text'}
+                            value={inputValue} onChange={step === 'intro_phone' ? handlePhoneChange : (e) => setInputValue(e.target.value)}
                             onKeyDown={(e) => e.key === 'Enter' && handleSend(inputValue)}
-                            placeholder="Escribe tu respuesta..." className="flex-1 bg-transparent border-none focus:ring-0 px-6 text-sm py-2 outline-none"
+                            placeholder={step === 'intro_phone' ? "(123) 456-7890" : "Escribe tu respuesta..."} className="flex-1 bg-transparent border-none focus:ring-0 px-6 text-sm py-2 outline-none"
                             disabled={isAnalyzing || step === 'completed'}
                         />
                         <button onClick={() => handleSend(inputValue)} disabled={isAnalyzing || step === 'completed'} className="w-10 h-10 bg-[#1C75BC] text-white rounded-full flex items-center justify-center shadow-md active:scale-95 transition-transform hover:bg-[#155a8a]">
diff --git a/client/src/components/interview/Fase12_Biometria.jsx b/client/src/components/interview/Fase12_Biometria.jsx
index 9638952..6242b64 100644
--- a/client/src/components/interview/Fase12_Biometria.jsx
+++ b/client/src/components/interview/Fase12_Biometria.jsx
@@ -38,19 +38,17 @@ const Fase12_Biometria = ({
 
     // DATOS RECOLECTADOS (ESTADO LOCAL)
     const [biometria, setBiometria] = useState({
-        vitales_antropometria: {
-            talla_m: patientData?.vitales_antropometria?.talla_m || null,
-            peso_kg: patientData?.vitales_antropometria?.peso_kg || null,
-            cintura_cm: patientData?.vitales_antropometria?.cintura_cm || null,
-            cadera_cm: patientData?.vitales_antropometria?.cadera_cm || null
-        },
-        signosVitales: {
-            ta: patientData?.signosVitales?.ta || "",
-            spo2: patientData?.signosVitales?.spo2 || "",
-            fc: patientData?.signosVitales?.fc || "",
-            temp: patientData?.signosVitales?.temp || "",
-            fr: patientData?.signosVitales?.fr || "",
-            glucosa: patientData?.signosVitales?.glucosa || ""
+        vitals: {
+            height: patientData?.vitals?.height || null,
+            weight: patientData?.vitals?.weight || null,
+            waist: patientData?.vitals?.waist || null,
+            hip: patientData?.vitals?.hip || null,
+            blood_pressure: patientData?.vitals?.blood_pressure || "",
+            spo2: patientData?.vitals?.spo2 || null,
+            hr: patientData?.vitals?.hr || null,
+            temperature: patientData?.vitals?.temperature || null,
+            rr: patientData?.vitals?.rr || null,
+            glucose: patientData?.vitals?.glucose || null
         }
     });
 
@@ -85,17 +83,16 @@ const Fase12_Biometria = ({
             const isValid = strictBooleanValidator(userMsg);
             if (isValid === false) {
                 // Skips biometrics completely
-                if (setPatientData) {
-                    setPatientData(prev => ({
-                        ...prev,
-                        vitales_antropometria: biometria.vitales_antropometria,
-                        signosVitales: biometria.signosVitales,
-                        clinical_flags: [...(prev.clinical_flags || []), ...clinicalFlags]
-                    }));
-                }
-                addBotMsg("Entendido.\\n\\nFase 7: Contexto Especial.\\n\\n┬┐Ha tenido cirug├¡as recientes, padece alg├║n s├¡ndrome o situaci├│n particular importante para su plan nutricional?");
+            if (setPatientData) {
+                setPatientData(prev => ({
+                    ...prev,
+                    vitals: { ...prev.vitals, ...biometria.vitals },
+                    clinical_flags: [...(prev.clinical_flags || []), ...clinicalFlags]
+                }));
+            }
+                addBotMsg("Entendido. Pasando a la siguiente secci├│n...");
                 setInternalStep('FINALIZED');
-                if (onPhaseComplete) onPhaseComplete('PHASE_14_SPECIAL_CONTEXT');
+                if (onPhaseComplete) onPhaseComplete('PHASE_13_SPECIAL_CONTEXT');
             } else if (isValid === true) {
                 addBotMsg(isYouth ? "┬┐Cu├íl es tu estatura en metros? (Ej: 1.65)" : "┬┐Cu├íl es su estatura en metros? (Ej: 1.65)");
                 setInternalStep('HEIGHT');
@@ -113,7 +110,7 @@ const Fase12_Biometria = ({
                 addBotMsg(isYouth ? "Por favor ingresa un n├║mero v├ílido (ej: 1.65)" : "Por favor ingrese un n├║mero v├ílido (ej: 1.65)");
                 return;
             }
-            setBiometria(prev => ({ ...prev, vitales_antropometria: { ...prev.vitales_antropometria, talla_m: val } }));
+            setBiometria(prev => ({ ...prev, vitals: { ...prev.vitals, height: val } }));
             addBotMsg(isYouth ? "┬┐Cu├íl es tu peso en kilogramos? (Ej: 65 o 65.5)" : "┬┐Cu├íl es su peso en kilogramos? (Ej: 65 o 65.5)");
             setInternalStep('WEIGHT');
         }
@@ -127,7 +124,7 @@ const Fase12_Biometria = ({
                 addBotMsg(isYouth ? "Por favor ingresa un n├║mero v├ílido (ej: 65.5)" : "Por favor ingrese un n├║mero v├ílido (ej: 65.5)");
                 return;
             }
-            setBiometria(prev => ({ ...prev, vitales_antropometria: { ...prev.vitales_antropometria, peso_kg: val } }));
+            setBiometria(prev => ({ ...prev, vitals: { ...prev.vitals, weight: val } }));
             addBotMsg(isYouth ? "┬┐Cu├íl es tu circunferencia de cintura en cent├¡metros? (Ej: 80)" : "┬┐Cu├íl es su circunferencia de cintura en cent├¡metros? (Ej: 80)");
             setInternalStep('WAIST');
         }
@@ -141,7 +138,7 @@ const Fase12_Biometria = ({
                 addBotMsg(isYouth ? "Por favor ingresa un n├║mero v├ílido (ej: 80)" : "Por favor ingrese un n├║mero v├ílido (ej: 80)");
                 return;
             }
-            setBiometria(prev => ({ ...prev, vitales_antropometria: { ...prev.vitales_antropometria, cintura_cm: val } }));
+            setBiometria(prev => ({ ...prev, vitals: { ...prev.vitals, waist: val } }));
             addBotMsg(isYouth ? "┬┐Cu├íl es tu circunferencia de cadera en cent├¡metros? (Ej: 95)" : "┬┐Cu├íl es su circunferencia de cadera en cent├¡metros? (Ej: 95)");
             setInternalStep('HIP');
         }
@@ -155,7 +152,7 @@ const Fase12_Biometria = ({
                 addBotMsg(isYouth ? "Por favor ingresa un n├║mero v├ílido (ej: 95)" : "Por favor ingrese un n├║mero v├ílido (ej: 95)");
                 return;
             }
-            setBiometria(prev => ({ ...prev, vitales_antropometria: { ...prev.vitales_antropometria, cadera_cm: val } }));
+            setBiometria(prev => ({ ...prev, vitals: { ...prev.vitals, hip: val } }));
             addBotMsg(isYouth ? "┬┐Cu├íl es tu presi├│n arterial? (Ej: 120/80)" : "┬┐Cu├íl es su presi├│n arterial? (Ej: 120/80)");
             setInternalStep('BP');
         }
@@ -191,7 +188,7 @@ const Fase12_Biometria = ({
             }
 
             if (newFlags.length > 0) setClinicalFlags(prev => [...prev, ...newFlags]);
-            setBiometria((prev) => ({ ...prev, signosVitales: { ...prev.signosVitales, ta: finalBP } }));
+            setBiometria((prev) => ({ ...prev, vitals: { ...prev.vitals, blood_pressure: finalBP } }));
 
             addBotMsg(`${alertMsg}${isYouth ? "┬┐Cu├íl es tu saturaci├│n de ox├¡geno? (Ej: 98)" : "┬┐Cu├íl es su saturaci├│n de ox├¡geno? (Ej: 98)"}`);
             setInternalStep('SPO2');
@@ -210,13 +207,13 @@ const Fase12_Biometria = ({
             let alertMsg = "";
             let newFlags = [];
             if (!isNaN(val)) {
-                setBiometria((prev) => ({ ...prev, signosVitales: { ...prev.signosVitales, spo2: val.toString() } }));
+                setBiometria((prev) => ({ ...prev, vitals: { ...prev.vitals, spo2: val } }));
                 if (val < 90) {
                     newFlags.push("HIPOXIA");
                     alertMsg = "­ƒÜ¿ ALERTA: HIPOXIA (SpO2 < 90%).\\n\\n";
                 }
             } else {
-                setBiometria((prev) => ({ ...prev, signosVitales: { ...prev.signosVitales, spo2: "N/A" } }));
+                setBiometria((prev) => ({ ...prev, vitals: { ...prev.vitals, spo2: null } }));
             }
 
             if (newFlags.length > 0) setClinicalFlags(prev => [...prev, ...newFlags]);
@@ -237,13 +234,13 @@ const Fase12_Biometria = ({
             let alertMsg = "";
             let newFlags = [];
             if (!isNaN(val)) {
-                setBiometria((prev) => ({ ...prev, signosVitales: { ...prev.signosVitales, fc: val.toString() } }));
+                setBiometria((prev) => ({ ...prev, vitals: { ...prev.vitals, hr: val } }));
                 if (val > 100) {
                     newFlags.push("TAQUICARDIA");
                     alertMsg = "­ƒÜ¿ ALERTA: TAQUICARDIA (FC > 100).\\n\\n";
                 }
             } else {
-                setBiometria((prev) => ({ ...prev, signosVitales: { ...prev.signosVitales, fc: "N/A" } }));
+                setBiometria((prev) => ({ ...prev, vitals: { ...prev.vitals, hr: null } }));
             }
 
             if (newFlags.length > 0) setClinicalFlags(prev => [...prev, ...newFlags]);
@@ -264,13 +261,13 @@ const Fase12_Biometria = ({
             let alertMsg = "";
             let newFlags = [];
             if (!isNaN(val)) {
-                setBiometria((prev) => ({ ...prev, signosVitales: { ...prev.signosVitales, temp: val.toString() } }));
+                setBiometria((prev) => ({ ...prev, vitals: { ...prev.vitals, temperature: val } }));
                 if (val > 37.5) {
                     newFlags.push("FIEBRE");
                     alertMsg = "­ƒÜ¿ ALERTA: FIEBRE (> 37.5┬░C).\\n\\n";
                 }
             } else {
-                setBiometria((prev) => ({ ...prev, signosVitales: { ...prev.signosVitales, temp: "N/A" } }));
+                setBiometria((prev) => ({ ...prev, vitals: { ...prev.vitals, temperature: null } }));
             }
 
             if (newFlags.length > 0) setClinicalFlags(prev => [...prev, ...newFlags]);
@@ -291,13 +288,13 @@ const Fase12_Biometria = ({
             let alertMsg = "";
             let newFlags = [];
             if (!isNaN(val)) {
-                setBiometria((prev) => ({ ...prev, signosVitales: { ...prev.signosVitales, fr: val.toString() } }));
+                setBiometria((prev) => ({ ...prev, vitals: { ...prev.vitals, rr: val } }));
                 if (val > 24) {
                     newFlags.push("TAQUIPNEA");
                     alertMsg = "­ƒÜ¿ ALERTA: TAQUIPNEA (> 24 rpm).\\n\\n";
                 }
             } else {
-                setBiometria((prev) => ({ ...prev, signosVitales: { ...prev.signosVitales, fr: "N/A" } }));
+                setBiometria((prev) => ({ ...prev, vitals: { ...prev.vitals, rr: null } }));
             }
 
             if (newFlags.length > 0) setClinicalFlags(prev => [...prev, ...newFlags]);
@@ -328,12 +325,12 @@ const Fase12_Biometria = ({
                 }
             }
 
-            const glucosaVal = (isNaN(val)) ? "No reportada" : val.toString() + " mg/dL";
+            const glucosaVal = (isNaN(val)) ? null : val;
 
             // Build the final internal state before committing externally
             const finalBio = {
                 ...biometria,
-                signosVitales: { ...biometria.signosVitales, glucosa: glucosaVal }
+                vitals: { ...biometria.vitals, glucose: glucosaVal }
             };
             setBiometria(finalBio);
 
@@ -344,15 +341,14 @@ const Fase12_Biometria = ({
             if (setPatientData) {
                 setPatientData(prev => ({
                     ...prev,
-                    vitales_antropometria: finalBio.vitales_antropometria,
-                    signosVitales: finalBio.signosVitales,
+                    vitals: { ...prev.vitals, ...finalBio.vitals },
                     clinical_flags: [...(prev.clinical_flags || []), ...finalFlags]
                 }));
             }
 
-            addBotMsg(`${alertMsg}­ƒôó Diga al paciente:\\n\\n${isYouth ? "'Perfecto, terminamos las mediciones. Regresate al escritorio. Ya tengo listo tu Diagn├│stico Integral.'\\n\\n(Fin de la Entrevista)." : "'Perfecto, terminamos las mediciones. Regresemos al escritorio. Ya tengo listo su Diagn├│stico Integral.'\\n\\n(Fin de la Entrevista)."}`);
+            addBotMsg(`${alertMsg}­ƒôó Diga al paciente:\\n\\n${isYouth ? "'Perfecto, terminamos las mediciones. Regresa al escritorio para unas ├║ltimas preguntas.'" : "'Perfecto, terminamos las mediciones. Regresemos al escritorio para unas ├║ltimas preguntas.'"}`);
             setInternalStep('FINALIZED');
-            if (onPhaseComplete) onPhaseComplete('PHASE_13_COMPLETE_HANDOFF'); // This maps to Phase 14 / Conclusion essentially in App architecture
+            if (onPhaseComplete) onPhaseComplete('PHASE_13_SPECIAL_CONTEXT');
         }
     };
 
diff --git a/client/src/components/interview/Fase13_ContextoEspecial.jsx b/client/src/components/interview/Fase13_ContextoEspecial.jsx
index 6d454dd..0f6d113 100644
--- a/client/src/components/interview/Fase13_ContextoEspecial.jsx
+++ b/client/src/components/interview/Fase13_ContextoEspecial.jsx
@@ -1,6 +1,7 @@
 import React, { useState, useEffect, useRef } from 'react';
 import ReactMarkdown from 'react-markdown';
 import tiloImg from '../../assets/tilo.png';
+import { motion } from 'framer-motion';
 
 const Fase13_ContextoEspecial = ({ setPatientData, isYouth, onPhaseComplete }) => {
     // ------------------------------------------------------------------------
@@ -50,8 +51,8 @@ const Fase13_ContextoEspecial = ({ setPatientData, isYouth, onPhaseComplete }) =
         // 2. Guardar el contexto en patientData
         setPatientData(prev => ({
             ...prev,
-            medical_history: {
-                ...prev.medical_history,
+            history: {
+                ...prev.history,
                 special_context: userText
             }
         }));
@@ -60,7 +61,7 @@ const Fase13_ContextoEspecial = ({ setPatientData, isYouth, onPhaseComplete }) =
         setTimeout(() => {
             const finalReply = {
                 role: "assistant",
-                content: "Excelente. He terminado de recabar todos los datos. Iniciare el an├ílisis para generar tu Diagn├│stico Integral.",
+                content: `Excelente. He terminado de recabar todos los datos. Iniciare el an├ílisis para generar ${isYouth ? 'tu' : 'su'} Diagn├│stico Integral.`,
                 avatar: tiloImg,
                 inputType: 'analyzing'
             };
diff --git a/client/src/components/interview/Fase14_Orquestador.jsx b/client/src/components/interview/Fase14_Orquestador.jsx
index c8de3e3..00f4d94 100644
--- a/client/src/components/interview/Fase14_Orquestador.jsx
+++ b/client/src/components/interview/Fase14_Orquestador.jsx
@@ -3,18 +3,23 @@ import React, { useState, useEffect } from 'react';
 import { motion, AnimatePresence } from 'framer-motion';
 import { BrainCircuit } from 'lucide-react';
 
-const Fase14_Orquestador = ({ onPhaseComplete }) => {
+const Fase14_Orquestador = ({ onPhaseComplete, patientData }) => {
     const [statusLog, setStatusLog] = useState([]);
     const [currentAction, setCurrentAction] = useState('Iniciando Cortex Integral...');
 
     useEffect(() => {
+        const isFemale = patientData?.profile?.sex === 'FEMENINO';
+        const riskLevel = patientData?.clinical_context?.alert_level || 'NORMAL';
+        
         const sequence = [
-            { time: 500, log: 'Procesando Eje Hormonal (Resistencia / Cortisol)...' },
-            { time: 2000, log: 'Analizando Psiquiatr├¡a Nutricional (Eje intestino-cerebro)...' },
-            { time: 3500, log: 'Sintetizando Medicina Metab├│lica (Inmunidad)...' },
-            { time: 5000, log: 'Evaluando Reequilibrio Biomec├ínico...' },
-            { time: 6500, log: 'Traduciendo a 7 Bloques Cl├¡nicos Estandarizados...' },
-            { time: 8000, action: 'done' }
+            { time: 500, log: `Iniciando an├ílisis para paciente ${isFemale ? 'femenina' : 'masculino'}...` },
+            { time: 2000, log: `Calibrando nivel de riesgo cl├¡nico: ${riskLevel}` },
+            { time: 3500, log: 'Procesando Eje Hormonal (Resistencia / Cortisol)...' },
+            { time: 5000, log: 'Analizando Psiquiatr├¡a Nutricional (Eje intestino-cerebro)...' },
+            { time: 6500, log: 'Sintetizando Medicina Metab├│lica (Inmunidad)...' },
+            { time: 8000, log: 'Evaluando Reequilibrio Biomec├ínico...' },
+            { time: 9500, log: 'Traduciendo a 7 Bloques Cl├¡nicos Estandarizados...' },
+            { time: 11000, action: 'done' }
         ];
 
         let timeouts = [];
diff --git a/client/src/components/interview/Fase15_SuplementacionAv.jsx b/client/src/components/interview/Fase15_SuplementacionAv.jsx
index 9a195fc..0abc875 100644
--- a/client/src/components/interview/Fase15_SuplementacionAv.jsx
+++ b/client/src/components/interview/Fase15_SuplementacionAv.jsx
@@ -34,7 +34,7 @@ const mockSuggestions = [
     }
 ];
 
-const Fase15_SuplementacionAv = ({ onPhaseComplete }) => {
+const Fase15_SuplementacionAv = ({ onPhaseComplete, patientData, setPatientData }) => {
     const [suggestions, setSuggestions] = useState(mockSuggestions);
     const [editingId, setEditingId] = useState(null);
     const [editForm, setEditForm] = useState({ dosage: '', timing: '' });
@@ -193,7 +193,15 @@ const Fase15_SuplementacionAv = ({ onPhaseComplete }) => {
 
             <div className="pt-6 mt-4 border-t border-slate-200">
                 <button
-                    onClick={() => onPhaseComplete('PHASE_16_DIETARY_PROTOCOL')}
+                    onClick={() => {
+                        if (setPatientData) {
+                            setPatientData(prev => ({
+                                ...prev,
+                                advanced_supplementation: suggestions.filter(s => s.status === 'approved')
+                            }));
+                        }
+                        onPhaseComplete('PHASE_16_DIETARY_PROTOCOL');
+                    }}
                     disabled={!allResolved}
                     className={`w-full py-4 rounded-xl font-bold flex items-center justify-center gap-2 transition-all ${allResolved
                         ? 'bg-black text-white hover:bg-slate-800 shadow-md transform hover:-translate-y-0.5'
diff --git a/client/src/components/interview/Fase16_ProtocoloDietetico.jsx b/client/src/components/interview/Fase16_ProtocoloDietetico.jsx
index 4c0ebe8..8053de4 100644
--- a/client/src/components/interview/Fase16_ProtocoloDietetico.jsx
+++ b/client/src/components/interview/Fase16_ProtocoloDietetico.jsx
@@ -1,7 +1,7 @@
 import React, { useState } from 'react';
 import { Calculator, Settings, Check, UtensilsCrossed, AlertTriangle } from 'lucide-react';
 
-const Fase16_ProtocoloDietetico = ({ onPhaseComplete }) => {
+const Fase16_ProtocoloDietetico = ({ onPhaseComplete, patientData, setPatientData }) => {
     const [tdee, setTdee] = useState('2200');
     const [target, setTarget] = useState('2000'); // D├®ficit por defecto
 
@@ -178,6 +178,20 @@ const Fase16_ProtocoloDietetico = ({ onPhaseComplete }) => {
                 <button
                     onClick={() => {
                         // Aqu├¡ se inyectar├¡a finalPatientData con Dieta
+                        if (setPatientData) {
+                            setPatientData(prev => ({
+                                ...prev,
+                                nutrition: {
+                                    ...prev.nutrition,
+                                    protocol: {
+                                        tdee,
+                                        target,
+                                        macros,
+                                        constraints
+                                    }
+                                }
+                            }));
+                        }
                         onPhaseComplete('PHASE_17_DASHBOARD_RENDER');
                     }}
                     disabled={totalMacros !== 100}
diff --git a/client/src/components/interview/Fase1_Identificacion.jsx b/client/src/components/interview/Fase1_Identificacion.jsx
index e7accf9..5d5d02d 100644
--- a/client/src/components/interview/Fase1_Identificacion.jsx
+++ b/client/src/components/interview/Fase1_Identificacion.jsx
@@ -2,7 +2,7 @@ import React, { useState, useEffect, useRef } from 'react';
 import { useClinicalGenome } from '../../store/useClinicalGenome';
 import { Send } from 'lucide-react';
 import { formatText } from '../../utils/utils';
-import { calculateAge, classifyLifeStage } from '../../utils/ageClassifier';
+import { calculateAge, classifyLifeStage, getBinaryGateLabels } from '../../utils/ageClassifier';
 import tiloImg from '../../assets/tilo.png';
 import SearchableVerticalMenu from '../ui/SearchableVerticalMenu';
 
@@ -30,14 +30,7 @@ export const Fase1_Identificacion = ({
     const [tempDob, setTempDob] = useState({ day: null, month: null, year: null });
 
     const handlePhoneChange = (e) => {
-        const input = e.target.value;
-        // Si el usuario est├í borrando, permitimos que el input quede tal cual para no arruinar el cursor
-        if (input.length < inputValue.length) {
-            setInputValue(input);
-            return;
-        }
-
-        let val = input.replace(/\D/g, '');
+        let val = e.target.value.replace(/\D/g, '');
         if (val.length > 10) val = val.slice(0, 10);
         let formatted = val;
         if (val.length > 6) {
@@ -223,11 +216,43 @@ export const Fase1_Identificacion = ({
                     nextStep = 'intro_religion';
                     break;
 
-                case 'intro_religion':
+                case 'intro_religion': {
                     updateSocioculturalProfile({ religion: cleanText });
-                    responseMsg = "Perfil Sociocultural completado. Transferiendo control...";
-                    nextStep = 'completed';
+                    
+                    const pInfo = identityLock.patientInfo;
+                    const socio = useClinicalGenome.getState().socioculturalProfile;
+                    const currentAge = pInfo.age || 20;
+                    const currentSex = pInfo.sex || "Femenino";
+                    
+                    const { confirmLabel, rejectLabel } = getBinaryGateLabels(currentAge, currentSex);
+                    
+                    responseMsg = `A continuaci├│n, le presento un resumen de los datos capturados:\n\n` +
+                                  `**Apellidos:** ${pInfo.apellidoPaterno} ${pInfo.apellidoMaterno}\n` +
+                                  `**Fecha de Nacimiento:** ${pInfo.dob_day}/${pInfo.dob_month}/${pInfo.dob_year} (${currentAge} a├▒os)\n` +
+                                  `**Sexo Biol├│gico:** ${currentSex}\n` +
+                                  `**Estado Civil:** ${socio.civilStatus || 'No especificado'}\n` +
+                                  `**Ocupaci├│n:** ${socio.occupation || 'No especificada'}\n` +
+                                  `**Religi├│n:** ${cleanText}\n\n` +
+                                  `┬┐Son correctos estos datos?`;
+                                  
+                    options = [
+                        { label: confirmLabel, value: "CONFIRM_DATA" },
+                        { label: rejectLabel, value: "CORRECT_DATA" }
+                    ];
+                    nextStep = 'intro_review';
+                    break;
+                }
+
+                case 'intro_review': {
+                    if (directValue === 'CONFIRM_DATA') {
+                        responseMsg = "Perfil Sociocultural completado. Transfiriendo control...";
+                        nextStep = 'completed';
+                    } else {
+                        responseMsg = "Entendido, vamos a corregir los datos.\n\n┬┐Cu├íl es su **Apellido Paterno**?";
+                        nextStep = 'intro_paterno';
+                    }
                     break;
+                }
 
                 default:
                     responseMsg = "Esta secci├│n ha concluido.";
diff --git a/client/src/components/interview/Fase2_Seguridad.jsx b/client/src/components/interview/Fase2_Seguridad.jsx
index eb6ca55..34b0230 100644
--- a/client/src/components/interview/Fase2_Seguridad.jsx
+++ b/client/src/components/interview/Fase2_Seguridad.jsx
@@ -2,6 +2,7 @@ import React, { useState, useEffect, useRef } from 'react';
 import { useClinicalGenome } from '../../store/useClinicalGenome';
 import { Zap, ShieldCheck } from 'lucide-react';
 import { formatText } from '../../utils/utils';
+import { getBinaryGateLabels } from '../../utils/ageClassifier';
 import tiloImg from '../../assets/tilo.png';
 
 export const Fase2_Seguridad = ({
@@ -128,8 +129,20 @@ export const Fase2_Seguridad = ({
                     }
 
                     updateIdentityLock({ emergencyContact: { ...identityLock.emergencyContact, phone: phoneInput }, verified: true });
-                    responseMsg = "Red de apoyo establecida exitosamente. Identidad Blindada completada. Ô£à";
-                    nextStep = 'completed';
+                    const eContact = { ...identityLock.emergencyContact, phone: phoneInput };
+                    const { confirmLabel, rejectLabel } = getBinaryGateLabels(patientAge, identityLock.patientInfo?.sex || 'Femenino');
+                    
+                    responseMsg = `A continuaci├│n, le presento un resumen de la Red de Apoyo capturada:\n\n` +
+                                  `**Contacto de Emergencia:** ${eContact.name}\n` +
+                                  `**Parentesco/Relaci├│n:** ${eContact.relation}\n` +
+                                  `**Tel├®fono de Emergencia:** ${eContact.phone}\n\n` +
+                                  `┬┐Son correctos estos datos?`;
+                                  
+                    options = [
+                        { label: confirmLabel, value: "CONFIRM_DATA" },
+                        { label: rejectLabel, value: "CORRECT_DATA" }
+                    ];
+                    nextStep = 'emergency_review';
                     break;
                 }
 
@@ -137,14 +150,37 @@ export const Fase2_Seguridad = ({
                     if (directValue === "CONFIRM_SAME_PHONE") {
                         const previousPhone = identityLock.patientInfo?.phone;
                         updateIdentityLock({ emergencyContact: { ...identityLock.emergencyContact, phone: previousPhone }, verified: true });
-                        responseMsg = "Entendido. Red de apoyo establecida. Identidad Blindada completada. Ô£à";
-                        nextStep = 'completed';
+                        const eContact = { ...identityLock.emergencyContact, phone: previousPhone };
+                        const { confirmLabel, rejectLabel } = getBinaryGateLabels(patientAge, identityLock.patientInfo?.sex || 'Femenino');
+                        
+                        responseMsg = `A continuaci├│n, le presento un resumen de la Red de Apoyo capturada:\n\n` +
+                                      `**Contacto de Emergencia:** ${eContact.name}\n` +
+                                      `**Parentesco/Relaci├│n:** ${eContact.relation}\n` +
+                                      `**Tel├®fono de Emergencia:** ${eContact.phone}\n\n` +
+                                      `┬┐Son correctos estos datos?`;
+                                      
+                        options = [
+                            { label: confirmLabel, value: "CONFIRM_DATA" },
+                            { label: rejectLabel, value: "CORRECT_DATA" }
+                        ];
+                        nextStep = 'emergency_review';
                     } else {
                         responseMsg = "De acuerdo. Por favor, escriba el **nuevo n├║mero** telef├│nico de emergencia (10 d├¡gitos):";
                         nextStep = 'emergency_phone';
                     }
                     break;
 
+                case 'emergency_review': {
+                    if (directValue === 'CONFIRM_DATA') {
+                        responseMsg = "Red de apoyo establecida exitosamente. Identidad Blindada completada. Ô£à";
+                        nextStep = 'completed';
+                    } else {
+                        responseMsg = "Entendido, vamos a corregir los datos.\n\n" + getStarterMessage();
+                        nextStep = 'emergency_name';
+                    }
+                    break;
+                }
+
                 default:
                     responseMsg = "Esta secci├│n ha concluido.";
                     break;
@@ -164,6 +200,20 @@ export const Fase2_Seguridad = ({
 
     useEffect(() => { chatEndRef.current?.scrollIntoView({ behavior: 'smooth' }); }, [messages]);
 
+    const handlePhoneChange = (e) => {
+        let val = e.target.value.replace(/\D/g, '');
+        if (val.length > 10) val = val.slice(0, 10);
+        let formatted = val;
+        if (val.length > 6) {
+            formatted = `(${val.slice(0, 3)}) ${val.slice(3, 6)}-${val.slice(6)}`;
+        } else if (val.length > 3) {
+            formatted = `(${val.slice(0, 3)}) ${val.slice(3)}`;
+        } else if (val.length > 0) {
+            formatted = `(${val}`;
+        }
+        setInputValue(formatted);
+    };
+
     return (
         <div className="flex h-full w-full bg-[#F8FAFC] overflow-hidden font-sans">
             <div className="w-full flex flex-col border-r border-slate-200 bg-white">
@@ -213,9 +263,9 @@ export const Fase2_Seguridad = ({
                     <div className="flex items-center gap-3 bg-slate-50 p-2 rounded-full border border-slate-200 focus-within:ring-2 focus-within:ring-blue-500 focus-within:bg-white transition-all shadow-inner">
                         <input
                             type={step.includes('phone') ? 'tel' : 'text'}
-                            value={inputValue} onChange={(e) => setInputValue(e.target.value)}
+                            value={inputValue} onChange={step.includes('phone') ? handlePhoneChange : (e) => setInputValue(e.target.value)}
                             onKeyDown={(e) => e.key === 'Enter' && handleSend(inputValue)}
-                            placeholder="Escribe tu respuesta..." className="flex-1 bg-transparent border-none focus:ring-0 px-6 text-sm py-2 outline-none"
+                            placeholder={step.includes('phone') ? "(123) 456-7890" : "Escribe tu respuesta..."} className="flex-1 bg-transparent border-none focus:ring-0 px-6 text-sm py-2 outline-none"
                             disabled={isAnalyzing || step === 'completed'}
                         />
                         <button onClick={() => handleSend(inputValue)} disabled={isAnalyzing || step === 'completed'} className="w-10 h-10 bg-[#1C75BC] text-white rounded-full flex items-center justify-center shadow-md active:scale-95 transition-transform hover:bg-[#155a8a]">
diff --git a/client/src/components/interview/Fase3_MotivoConsulta.jsx b/client/src/components/interview/Fase3_MotivoConsulta.jsx
index 2af4212..a2e8834 100644
--- a/client/src/components/interview/Fase3_MotivoConsulta.jsx
+++ b/client/src/components/interview/Fase3_MotivoConsulta.jsx
@@ -9,21 +9,21 @@ import { analyzeClinicalMotive } from '../../hooks/useCortex';
 
 const motiveOptions = [
     { label: "Adicciones y Sustancias", value: "GOAL_ADDICTIONS" },
-    { label: "Adulto Mayor (GeriatrÔö£┬ía)", value: "GOAL_GERIATRICS" },
+    { label: "Adulto Mayor (Geriatr├¡a)", value: "GOAL_GERIATRICS" },
     { label: "Alergias Graves (Protocolo Anafilaxia)", value: "GOAL_ALLERGIES" },
     { label: "Bajar de Peso / Sobrepeso", value: "GOAL_WEIGHT_LOSS" },
-    { label: "BariÔö£├¡trica / QuirÔö£Ôòærgico", value: "GOAL_BARIATRIC" },
+    { label: "Bari├ítrica / Quir├║rgico", value: "GOAL_BARIATRIC" },
     { label: "Climaterio y Menopausia", value: "GOAL_MENOPAUSE" },
-    { label: "Control ClÔö£┬ínico (PatologÔö£┬ías CrÔö£Ôöénicas)", value: "GOAL_CLINICAL" },
+    { label: "Control Cl├¡nico (Patolog├¡as Cr├│nicas)", value: "GOAL_CLINICAL" },
     { label: "Cuidados Paliativos", value: "GOAL_PALLIATIVE" },
-    { label: "Discapacidad y RehabilitaciÔö£Ôöén", value: "GOAL_DISABILITY" },
+    { label: "Discapacidad y Rehabilitaci├│n", value: "GOAL_DISABILITY" },
     { label: "Embarazo y Lactancia", value: "GOAL_PREGNANCY" },
-    { label: "Ganar MÔö£Ôòæsculo / Deporte (Rendimiento)", value: "GOAL_MUSCLE" },
-    { label: "OncologÔö£┬ía Nutricional", value: "GOAL_ONCOLOGY" },
-    { label: "PediatrÔö£┬ía (Crecimiento y Desarrollo)", value: "GOAL_PEDIATRICS" },
-    { label: "PrevenciÔö£Ôöén y Longevidad (Biohacking)", value: "GOAL_LONGEVITY" },
+    { label: "Ganar M├║sculo / Deporte (Rendimiento)", value: "GOAL_MUSCLE" },
+    { label: "Oncolog├¡a Nutricional", value: "GOAL_ONCOLOGY" },
+    { label: "Pediatr├¡a (Crecimiento y Desarrollo)", value: "GOAL_PEDIATRICS" },
+    { label: "Prevenci├│n y Longevidad (Biohacking)", value: "GOAL_LONGEVITY" },
     { label: "Salud Mental / TCA (Seguridad Conductual)", value: "GOAL_MENTAL_HEALTH" },
-    { label: "Salud Renal (NefropatÔö£┬ía)", value: "GOAL_RENAL" },
+    { label: "Salud Renal (Nefropat├¡a)", value: "GOAL_RENAL" },
     { label: "VIH e Inmunodeficiencias", value: "GOAL_IMMUNE" }
 ];
 
@@ -95,7 +95,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
             return true;
         }).map(opt => {
             if (isMinor && opt.value === 'GOAL_IMMUNE') {
-                return { ...opt, label: "InmunologÔö£┬ía PediÔö£├¡trica" };
+                return { ...opt, label: "Inmunolog├¡a Pedi├ítrica" };
             }
             return opt;
         }).sort((a, b) => a.label.localeCompare(b.label));
@@ -129,8 +129,8 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                 const cleanName = patientName !== "NOM" ? patientName : "el paciente";
 
                 const welcomeMsg = isMinor
-                    ? `Para diseÔö£ÔûÆar el plan clÔö£┬ínico de **${cleanName}**, por favor seleccione la **Ruta Primaria** o Motivo de Consulta principal:`
-                    : "Para comenzar a diseÔö£ÔûÆar su plan clÔö£┬ínico de precisiÔö£Ôöén, por favor seleccione su **Ruta Primaria** o Motivo de Consulta principal:";
+                    ? `Para dise├▒ar el plan cl├¡nico de **${cleanName}**, por favor seleccione la **Ruta Primaria** o Motivo de Consulta principal:`
+                    : "Para comenzar a dise├▒ar su plan cl├¡nico de precisi├│n, por favor seleccione su **Ruta Primaria** o Motivo de Consulta principal:";
 
                 setMessages(prev => [
                     ...prev,
@@ -212,10 +212,10 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                         ];
 
                         if (SKIP_BODY_MAP_GOALS.includes(userMsg)) {
-                            responseMsg = `Entendido. Hemos activado la ruta clÔö£┬ínica de **${label}**.\n\nÔö¼ÔöÉPodrÔö£┬ía describir brevemente cualquier sÔö£┬íntoma o detalle importante que debamos considerar para la consulta?`;
+                            responseMsg = `Entendido. Hemos activado la ruta cl├¡nica de **${label}**.\n\nÔö¼ÔöÉPodr├¡a describir brevemente cualquier s├¡ntoma o detalle importante que debamos considerar para la consulta?`;
                             nextStep = 'clinica_triage_symptoms';
                         } else {
-                            responseMsg = `Entendido. Hemos activado la ruta clÔö£┬ínica de **${label}**.\n\nPara ser mÔö£├¡s precisos, por favor **indique en el mapa** dÔö£Ôöénde siente mayor molestia o si hay zonas especÔö£┬íficas a tratar. Haga clic en *Completar Mapa* cuando termine.`;
+                            responseMsg = `Entendido. Hemos activado la ruta cl├¡nica de **${label}**.\n\nPara ser m├ís precisos, por favor **indique en el mapa** d├│nde siente mayor molestia o si hay zonas espec├¡ficas a tratar. Haga clic en *Completar Mapa* cuando termine.`;
                             nextStep = 'clinica_body_map';
                         }
                     } else {
@@ -223,7 +223,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                         const cleanText = formatText(userMsg);
                         
                         updateClinicalContext({
-                            primary_motive: "Inferencia SemÔö£├¡ntica GEM",
+                            primary_motive: "Inferencia Sem├íntica GEM",
                             secondary_symptoms: cleanText,
                             history: [...(patientData.clinical_context?.history || []), {
                                 question: "Motivo de Consulta (Texto Libre)",
@@ -232,7 +232,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                             }]
                         });
 
-                        const highSeverityKeywords = ['cancer', 'cÔö£├¡ncer', 'matriz', 'amputa', 'duelo', 'falleci', 'muerte', 'perdÔö£┬í', 'tumor', 'maligno', 'quimio'];
+                        const highSeverityKeywords = ['cancer', 'c├íncer', 'matriz', 'amputa', 'duelo', 'falleci', 'muerte', 'perd├¡', 'tumor', 'maligno', 'quimio'];
                         const isHighSeverity = highSeverityKeywords.some(kw => cleanText.toLowerCase().includes(kw));
 
                         if (isHighSeverity) {
@@ -243,7 +243,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
 
                         // AI Semantic Triage (GEM Integration)
                         setStep('clinica_triage_ai_analysis');
-                        setMessages(prev => [...prev, { role: "assistant", content: "T.I.L.O. estÔö£├¡ procesando tus biosensores y realizando el cruce forense de tu motivo de consulta..." }]);
+                        setMessages(prev => [...prev, { role: "assistant", content: "T.I.L.O. est├í procesando tus biosensores y realizando el cruce forense de tu motivo de consulta..." }]);
                         
                         const telemetry = { 
                             age: age, 
@@ -266,7 +266,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                              
                              let finalResponse = aiResult.reasoning;
                              if (aiResult.redFlag) {
-                                 finalResponse += "\n\n├ö├£├í┬┤┬®├à **Protocolo de DerivaciÔö£Ôöén Activo por Alerta ClÔö£┬ínica (Red Flag).**";
+                                 finalResponse += "\n\nÔÜá´©Å **Protocolo de Derivaci├│n Activo por Alerta Cl├¡nica (Red Flag).**";
                              }
 
                              setMessages(prev => [...prev, { role: "assistant", content: finalResponse }]);
@@ -280,7 +280,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
 
                 case 'clinica_body_map': {
                     if (userMsg === 'BODY_MAP_COMPLETE') {
-                        responseMsg = "Entendido. Ôö¼ÔöÉPodrÔö£┬ía describir brevemente cualquier otro sÔö£┬íntoma o detalle importante que no aparezca en el mapa?";
+                        responseMsg = "Entendido. Ôö¼ÔöÉPodr├¡a describir brevemente cualquier otro s├¡ntoma o detalle importante que no aparezca en el mapa?";
                         nextStep = 'clinica_triage_symptoms';
                     }
                     break;
@@ -292,13 +292,13 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                     updateClinicalContext({
                         secondary_symptoms: (patientData.clinical_context?.secondary_symptoms || "") + symptomsInput,
                         history: [...(patientData.clinical_context?.history || []), {
-                            question: "SintomatologÔö£┬ía Adicional",
+                            question: "Sintomatolog├¡a Adicional",
                             answer: symptomsInput,
                             timestamp: new Date().toISOString()
                         }]
                     });
 
-                    const highSeverityKeywords = ['cancer', 'cÔö£├¡ncer', 'matriz', 'amputa', 'duelo', 'falleci', 'muerte', 'perdÔö£┬í', 'tumor', 'maligno', 'quimio'];
+                    const highSeverityKeywords = ['cancer', 'c├íncer', 'matriz', 'amputa', 'duelo', 'falleci', 'muerte', 'perd├¡', 'tumor', 'maligno', 'quimio'];
                     const sensitiveKeywords = ['quiste', 'biopsia', 'seno', 'mama', 'oncologo'];
                     const surgeryKeywords = ['operacion', 'cirugia', 'cesarea', 'apendice', 'vesicula', 'histerectomia'];
 
@@ -311,15 +311,15 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                         setTimeout(() => handleContainmentSequence(symptomsInput), 100);
                         return; 
                     } else if (isSensitive) {
-                        responseMsg = "Entiendo la importancia de lo que menciona. Para poder apoyarle mejor, Ôö¼ÔöÉle gustarÔö£┬ía compartir un poco mÔö£├¡s sobre este diagnÔö£Ôöéstico o prefiere que lo abordemos con detalle directamente en la consulta?";
+                        responseMsg = "Entiendo la importancia de lo que menciona. Para poder apoyarle mejor, Ôö¼ÔöÉle gustar├¡a compartir un poco m├ís sobre este diagn├│stico o prefiere que lo abordemos con detalle directamente en la consulta?";
                         nextStep = 'clinica_triage_sensitive_followup';
                     } else if (isSurgery) {
-                        responseMsg = "Entendido. Dado que menciona un procedimiento quirÔö£Ôòærgico, Ôö¼ÔöÉhace cuÔö£├¡nto tiempo fue o cuÔö£├¡ndo estÔö£├¡ programado?";
+                        responseMsg = "Entendido. Dado que menciona un procedimiento quir├║rgico, Ôö¼ÔöÉhace cu├ínto tiempo fue o cu├índo est├í programado?";
                         nextStep = 'intro_triage_surgery';
                     } else {
                         // AI Semantic Triage (GEM Integration)
                         setStep('clinica_triage_ai_analysis');
-                        setMessages(prev => [...prev, { role: "assistant", content: "T.I.L.O. estÔö£├¡ procesando tus biosensores y evaluaciÔö£Ôöén clÔö£┬ínica..." }]);
+                        setMessages(prev => [...prev, { role: "assistant", content: "T.I.L.O. est├í procesando tus biosensores y evaluaci├│n cl├¡nica..." }]);
                         
                         const freeText = (patientData.clinical_context?.primary_motive || "") + " " + (patientData.clinical_context?.secondary_symptoms || "") + " " + symptomsInput;
                         const telemetry = { 
@@ -342,9 +342,9 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                              });
                              
                              if (aiResult.redFlag) {
-                                 setMessages(prev => [...prev, { role: "assistant", content: `├ö├£├í┬┤┬®├à ${aiResult.reasoning}\n\nActivando protocolo de derivaciÔö£Ôöén y evaluaciÔö£Ôöén mÔö£┬«dica prioritaria.` }]);
+                                 setMessages(prev => [...prev, { role: "assistant", content: `ÔÜá´©Å ${aiResult.reasoning}\n\nActivando protocolo de derivaci├│n y evaluaci├│n m├®dica prioritaria.` }]);
                              } else {
-                                 setMessages(prev => [...prev, { role: "assistant", content: `├ö┬ú├á ${aiResult.reasoning}` }]);
+                                 setMessages(prev => [...prev, { role: "assistant", content: `Ô£à ${aiResult.reasoning}` }]);
                              }
                              setStep('clinica_triage_ai_complete');
                         });
@@ -365,7 +365,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                     });
 
                     setStep('clinica_triage_ai_analysis');
-                    setMessages(prev => [...prev, { role: "assistant", content: "T.I.L.O. estÔö£├¡ integrando estos detalles sensibles en su matriz de evaluaciÔö£Ôöén..." }]);
+                    setMessages(prev => [...prev, { role: "assistant", content: "T.I.L.O. est├í integrando estos detalles sensibles en su matriz de evaluaci├│n..." }]);
                     
                     const telemetry = { age: age, gender: gender, location: patientData?.profile?.address?.state || "No especificado" };
                     const bodyMapZones = patientData.clinical_context?.pain_zones || [];
@@ -382,7 +382,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                              }
                          });
                          
-                         setMessages(prev => [...prev, { role: "assistant", content: `├ö┬ú├á ${aiResult.reasoning}` }]);
+                         setMessages(prev => [...prev, { role: "assistant", content: `Ô£à ${aiResult.reasoning}` }]);
                          setStep('clinica_triage_ai_complete');
                     });
                     return;
@@ -395,15 +395,15 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
 
                     if (lowerMsg.includes('pre') || lowerMsg.includes('prepara') || lowerMsg.includes('antes') || lowerMsg.includes('programada')) {
                         status = "PRE";
-                        msg = "├ö├£├í┬┤┬®├à ALERTA: Suspender suplementos anticoagulantes (Ajo, Omega-3, Ginkgo) 7 dÔö£┬ías antes.";
-                    } else if (lowerMsg.includes('ya') || lowerMsg.includes('post') || lowerMsg.includes('pasÔö£Ôöé') || lowerMsg.includes('paso')) {
+                        msg = "ÔÜá´©Å ALERTA: Suspender suplementos anticoagulantes (Ajo, Omega-3, Ginkgo) 7 d├¡as antes.";
+                    } else if (lowerMsg.includes('ya') || lowerMsg.includes('post') || lowerMsg.includes('pas├│') || lowerMsg.includes('paso')) {
                         status = "POST";
-                        msg = "├ö├£├í┬┤┬®├à ALERTA: Validar alta mÔö£┬«dica antes de iniciar esfuerzo fÔö£┬ísico.";
+                        msg = "ÔÜá´©Å ALERTA: Validar alta m├®dica antes de iniciar esfuerzo f├¡sico.";
                     }
 
                     updateClinicalContext({
                         history: [...(patientData.clinical_context?.history || []), {
-                            question: "Estatus QuirÔö£Ôòærgico",
+                            question: "Estatus Quir├║rgico",
                             answer: msg ? `${status} (${msg})` : "Sin Intervenciones Recientes",
                             timestamp: new Date().toISOString()
                         }]
@@ -439,13 +439,13 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
     };
 
     const handleContainmentSequence = (triggerText) => {
-        const sensitiveKeywords = ['cancer', 'cÔö£├¡ncer', 'tumor', 'falleci', 'muerte', 'matriz', 'duelo', 'luto', 'perdida', 'pÔö£┬«rdida'];
+        const sensitiveKeywords = ['cancer', 'c├íncer', 'tumor', 'falleci', 'muerte', 'matriz', 'duelo', 'luto', 'perdida', 'p├®rdida'];
         const matchedKw = sensitiveKeywords.find(kw => triggerText.toLowerCase().includes(kw)) || "Tema Sensible";
 
         updateClinicalContext({
             history: [...(patientData.clinical_context?.history || []), {
-                question: "├ö├£├í┬┤┬®├à Reporte de Sensibilidad",
-                answer: `Tema identificado: "${matchedKw.toUpperCase()}". Protocolo de contenciÔö£Ôöén activado.`,
+                question: "ÔÜá´©Å Reporte de Sensibilidad",
+                answer: `Tema identificado: "${matchedKw.toUpperCase()}". Protocolo de contenci├│n activado.`,
                 timestamp: new Date().toISOString()
             }]
         });
@@ -458,7 +458,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
             setIsTyping(false);
             setMessages(prev => [...prev, {
                 role: "assistant",
-                content: `${nameStr}, agradezco profundamente su confianza al compartirme algo tan personal. Lamento mucho que estÔö£┬« pasando por este proceso de incertidumbre; entiendo que una noticia asÔö£┬í genera mucha preocupaciÔö£Ôöén.`
+                content: `${nameStr}, agradezco profundamente su confianza al compartirme algo tan personal. Lamento mucho que est├® pasando por este proceso de incertidumbre; entiendo que una noticia as├¡ genera mucha preocupaci├│n.`
             }]);
 
             setIsTyping(true);
@@ -466,7 +466,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                 setIsTyping(false);
                 setMessages(prev => [...prev, {
                     role: "assistant",
-                    content: "He marcado este dato como Prioridad MÔö£├¡xima en su expediente. Su nutriÔö£Ôöélogo abordarÔö£├¡ el tema con toda la sensibilidad y el cuidado que usted merece desde el primer minuto de la consulta."
+                    content: "He marcado este dato como Prioridad M├íxima en su expediente. Su nutri├│logo abordar├í el tema con toda la sensibilidad y el cuidado que usted merece desde el primer minuto de la consulta."
                 }]);
 
                 setTimeout(() => {
@@ -507,12 +507,12 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
 
         const zoneLabels = {
             'M_HEAD': 'Cabeza', 'F_HEAD': 'Cabeza',
-            'M_LUNGS_R': 'PulmÔö£Ôöén Derecho', 'M_LUNGS_L': 'PulmÔö£Ôöén Izquierdo',
-            'F_LUNG_R': 'PulmÔö£Ôöén Derecho', 'F_LUNG_L': 'PulmÔö£Ôöén Izquierdo',
+            'M_LUNGS_R': 'Pulm├│n Derecho', 'M_LUNGS_L': 'Pulm├│n Izquierdo',
+            'F_LUNG_R': 'Pulm├│n Derecho', 'F_LUNG_L': 'Pulm├│n Izquierdo',
             'F_BREAST_R': 'Seno Derecho', 'F_BREAST_L': 'Seno Izquierdo',
             'F_OVARY_R': 'Ovario Derecho', 'F_OVARY_L': 'Ovario Izquierdo',
-            'F_KIDNEY_R': 'RiÔö£ÔûÆÔö£Ôöén Derecho', 'F_KIDNEY_L': 'RiÔö£ÔûÆÔö£Ôöén Izquierdo',
-            'M_KIDNEY_R': 'RiÔö£ÔûÆÔö£Ôöén Derecho', 'M_KIDNEY_L': 'RiÔö£ÔûÆÔö£Ôöén Izquierdo'
+            'F_KIDNEY_R': 'Ri├▒├│n Derecho', 'F_KIDNEY_L': 'Ri├▒├│n Izquierdo',
+            'M_KIDNEY_R': 'Ri├▒├│n Derecho', 'M_KIDNEY_L': 'Ri├▒├│n Izquierdo'
         };
 
         const newTags = zones.map(z => tagMap[z] || 'PAIN_GENERAL');
@@ -536,10 +536,10 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
         setMessages(prev => [...prev, { role: "user", content: `Zonas: ${summary} | Intensidad: ${intensity}/10` }]);
 
         if (isRedFlag) {
-            setMessages(prev => [...prev, { role: "assistant", content: "├ö├£├í┬┤┬®├à **ALERTA DE SEGURIDAD**: He detectado un nivel de dolor severo en una zona sensible. \n\nÔö¼ÔöÉPodrÔö£┬ía describir brevemente cualquier otro sÔö£┬íntoma o detalle importante que no aparezca en el mapa?" }]);
+            setMessages(prev => [...prev, { role: "assistant", content: "ÔÜá´©Å **ALERTA DE SEGURIDAD**: He detectado un nivel de dolor severo en una zona sensible. \n\nÔö¼ÔöÉPodr├¡a describir brevemente cualquier otro s├¡ntoma o detalle importante que no aparezca en el mapa?" }]);
             setStep('clinica_triage_symptoms');
         } else {
-            setMessages(prev => [...prev, { role: "assistant", content: "Entendido. Ôö¼ÔöÉPodrÔö£┬ía describir brevemente cualquier otro sÔö£┬íntoma o detalle importante que no aparezca en el mapa?" }]);
+            setMessages(prev => [...prev, { role: "assistant", content: "Entendido. Ôö¼ÔöÉPodr├¡a describir brevemente cualquier otro s├¡ntoma o detalle importante que no aparezca en el mapa?" }]);
             setStep('clinica_triage_symptoms');
         }
     };
@@ -587,7 +587,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                             onClick={(e) => { e.preventDefault(); handleSend(); }}
                             className="px-6 py-3 bg-tilo-primary text-white rounded-2xl font-medium shadow-md hover:shadow-lg hover:scale-105 transition-all flex items-center gap-2"
                         >
-                            Continuar a Historial ClÔö£┬ínico <Send className="w-4 h-4" />
+                            Continuar a Historial Cl├¡nico <Send className="w-4 h-4" />
                         </button>
                     </div>
                 )}
@@ -598,7 +598,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
                         value={inputValue}
                         onChange={(e) => setInputValue(e.target.value)}
                         onKeyDown={(e) => e.key === 'Enter' && handleSend()}
-                        placeholder={step === 'clinica_body_map' ? "Seleccione una opciÔö£Ôöén..." : (step === 'clinica_triage_start' ? "Seleccione o escriba su sÔö£┬íntoma libremente..." : dynamicPlaceholder)}
+                        placeholder={step === 'clinica_body_map' ? "Seleccione una opci├│n..." : (step === 'clinica_triage_start' ? "Seleccione o escriba su s├¡ntoma libremente..." : dynamicPlaceholder)}
                         className="flex-1 bg-transparent outline-none text-tilo-text-main placeholder:text-tilo-text-muted text-sm h-10 px-4"
                         disabled={isTyping || step === 'clinica_body_map' || step === 'clinica_triage_containment' || step === 'clinica_triage_ai_analysis' || step === 'clinica_triage_ai_complete' || step === 'clinica_triage_containment_complete'}
                     />
@@ -613,7 +613,7 @@ const Fase3_MotivoConsulta = ({ setMessages, onPhaseComplete, patientData, setPa
 
                 <div className="text-center mt-2 text-xs text-tilo-text-muted font-sansation flex items-center justify-center gap-2">
                     <i className="fi fi-rr-shield-check"></i>
-                    Terminal A - ComunicaciÔö£Ôöén ClÔö£┬ínica Encriptada Extremo a Extremo
+                    Terminal A - Comunicaci├│n Cl├¡nica Encriptada Extremo a Extremo
                 </div>
             </div>
         </div>
diff --git a/client/src/hooks/useCortex.js b/client/src/hooks/useCortex.js
index 03174d9..b105f35 100644
--- a/client/src/hooks/useCortex.js
+++ b/client/src/hooks/useCortex.js
@@ -56,7 +56,7 @@ const INITIAL_PATIENT_DATA = {
         r24h: [],
         ffq: {}
     },
-    // Fases 4, 5, 6, 7
+    // Fases 4, 5, 6, 7, 13
     history: {
         family_raw_text: "",
         family_checklist_verified: false,
@@ -71,7 +71,9 @@ const INITIAL_PATIENT_DATA = {
         },
         // Fase 8
         digestive_symptoms: [],
-        digestive_frequency: ""
+        digestive_frequency: "",
+        // Fase 13
+        special_context: ""
     },
     // Fase 9
     physio: {
@@ -92,24 +94,35 @@ const INITIAL_PATIENT_DATA = {
     activity: {
         exercise: { has_scheduled_exercise: false, log: [] }
     },
-    // Fases 12, 13, 14
+    // Fases 12, 13, 14, 16
     nutrition: {
         cook_type: "",
         venue: "",
-        preferences: { dislikes: [], favorites: [] }
+        preferences: { dislikes: [], favorites: [] },
+        protocol: {}
     },
     logistics: {
         chronobiology: { timeline: [] }
     },
-    // Fases 16, 17, 18
+    // Fase 15
+    advanced_supplementation: [],
+    // Fases 12, 16, 17, 18
     vitals: {
         weight: null,
         height: null,
-        blood_pressure: ""
+        blood_pressure: "",
+        waist: null,
+        hip: null,
+        hr: null,
+        spo2: null,
+        temperature: null,
+        rr: null,
+        glucose: null
     },
     biochemical: {
         electret_scan_data: {}
     },
+    clinical_flags: [], // Acoplamiento estabilizado para alertas Fase 12
     // FASE 13 (Biom├®tricos legacy Fase 6)
     peso: "",
     talla: "",
@@ -141,7 +154,8 @@ const INITIAL_PATIENT_DATA = {
         pain_zones: [],
         intensity: 0,
         ai_analysis: { detected_tags: [] }
-    }
+    },
+    clinical_flags: []
 };
 
 
@@ -149,6 +163,61 @@ const INITIAL_PATIENT_DATA = {
  * useCortex - Motor de L├│gica Conversacional T.I.L.O.
  * Maneja el estado de la entrevista cl├¡nica interactiva y sincroniza el Dashboard.
  */
+
+export const analyzeClinicalMotive = async (freeText, telemetry, bodyMapZones = []) => {
+    try {
+        const apiUrl = import.meta.env.VITE_API_URL || 'http://localhost:5000';
+        
+        const response = await fetch(`${apiUrl}/api/cortex/analyzeMotive`, {
+            method: 'POST',
+            headers: {
+                'Content-Type': 'application/json'
+            },
+            body: JSON.stringify({
+                freeText,
+                telemetry,
+                bodyMapZones
+            })
+        });
+
+        if (!response.ok) {
+            throw new Error('Cortex API responded with an error');
+        }
+
+        const data = await response.json();
+        
+        let alertLevel = data.redFlag ? "CRITICAL" : "NONE";
+        
+        // Escalar alertas de forma estructural si el motor IA detecta categor├¡as de alto riesgo
+        if (data.category === "ONCOLOGY") alertLevel = "CRITICAL";
+        else if (data.category === "SURGICAL" && alertLevel === "NONE") alertLevel = "WARNING";
+
+        return {
+            category: data.category || "CLINICAL",
+            alert: alertLevel,
+            suspicion: data.primaryRoute, // Usamos la ruta devuelta por IA como sospecha principal
+            isGoal: data.isGoal || false,
+            isPregnant: data.isPregnant || false,
+            primaryRoute: data.primaryRoute,
+            secondaryRoute: data.secondaryRoute,
+            reasoning: data.reasoning
+        };
+
+    } catch (error) {
+        console.error("Neural Cortex Analysis Error:", error);
+        return {
+            category: "CLINICAL",
+            alert: "NONE",
+            suspicion: "Inferencia Sem├íntica",
+            isGoal: false,
+            isPregnant: false,
+            primaryRoute: "GOAL_EDUCATION",
+            secondaryRoute: null,
+            reasoning: "Fallback preventivo por falla en Neural Cortex"
+        };
+    }
+};
+
 export const useCortex = () => {
     const { validateCitation } = useCitationValidation();
     // 0. LOCAL STORAGE RECOVERY FUNCTION
@@ -1636,28 +1705,8 @@ export const useCortex = () => {
                     }));
 
                     // TRANSITION TO FASE 3: MOTIVO DE CONSULTA
-                    const ptCtx = patientData.profile.pediatric_profile;
-                    const isYouth = ptCtx?.ui_controls?.tone_key === 'YOUTH_EMP_TONE';
-                    const msgContent = isYouth
-                        ? "Perfecto. Historial registrado correctamente.\n\nPara optimizar tu expediente con Inteligencia Artificial, necesito saber: **┬┐Cu├íl es el objetivo principal de tu visita hoy?**"
-                        : "Perfecto. Historial registrado correctamente.\n\nPara optimizar nuestro algoritmo cl├¡nico, necesito saber: **┬┐Cu├íl es el objetivo principal de su visita hoy?**";
-
-                    setMessages(prev => [...prev, {
-                        role: 'assistant',
-                        content: msgContent,
-                        avatar: tiloImg,
-                        inputType: 'buttons',
-                        options: [
-                            { label: "ÔÜû´©Å Bajar de Peso", value: "GOAL_WEIGHT_LOSS" },
-                            { label: "­ƒÆ¬ Ganar M├║sculo", value: "GOAL_MUSCLE" },
-                            { label: "­ƒÅà Rendimiento Deportivo", value: "GOAL_SPORT" },
-                            { label: "­ƒ®║ Control Cl├¡nico", value: "GOAL_CLINICAL" },
-                            { label: "­ƒñ░ Etapa de Vida", value: "GOAL_LIFE_STAGE" },
-                            { label: "­ƒÑù Aprender a Comer", value: "GOAL_EDUCATION" }
-                        ]
-                    }]);
-                    setActiveTab('clinical'); // Update UI Context to Clinical History Tab
-                    setCurrentPhase('PHASE_3_OPEN_PROMPT');
+                    setActiveTab('profile'); // Update UI Context to Clinical History Tab
+                    setCurrentPhase('PHASE_3_MOTIVO_CONSULTA');
                     break;
                 }
 
@@ -1797,7 +1846,7 @@ export const useCortex = () => {
                             { label: "ÔØî No", value: "NO_HEREDO" }
                         ]
                     }]);
-                    setActiveTab('clinical');
+                    setActiveTab('profile');
                     setCurrentPhase('PHASE_4_HEREDO_CONFIRM');
                     break;
                 }
@@ -1982,7 +2031,7 @@ export const useCortex = () => {
                             { label: "Ninguna", value: "Ninguna" }
                         ]
                     }]);
-                    setActiveTab('clinical'); // Ensured UI consistency
+                    setActiveTab('profile'); // Ensured UI consistency
                     setCurrentPhase('PHASE_5_SAFETY_GATE');
                     break;
                 }
diff --git a/client/src/utils/ageClassifier.js b/client/src/utils/ageClassifier.js
index 3ca8033..d4bdd9a 100644
--- a/client/src/utils/ageClassifier.js
+++ b/client/src/utils/ageClassifier.js
@@ -44,3 +44,40 @@ export const calculateAge = (day, month, year) => {
 
     return age;
 };
+
+/**
+ * Retorna las etiquetas de la compuerta binaria de confirmaci├│n seg├║n la NOM-004.
+ * Basado en BINARY_GATE_LOGIC V37.1
+ * @param {number} age - Edad en a├▒os
+ * @param {string} sex - "Femenino" o "Masculino"
+ * @returns {object} { confirmLabel, rejectLabel }
+ */
+export const getBinaryGateLabels = (age, sex) => {
+    const isFemale = sex === 'Femenino';
+    let confirmLabel = "Ô£à S├ì, ES CORRECTO"; // Default fallback
+    
+    if (age < 1) {
+        // Neonato
+        confirmLabel = isFemale ? "Ô£à S├ì, ES LA RECI├ëN NACIDA" : "Ô£à S├ì, ES EL RECI├ëN NACIDO";
+    } else if (age >= 1 && age < 3) {
+        // Lactante
+        confirmLabel = isFemale ? "Ô£à S├ì, ES ELLA" : "Ô£à S├ì, ES ├ëL";
+    } else if (age >= 3 && age < 13) {
+        // Pedi├ítrico
+        confirmLabel = isFemale ? "Ô£à S├ì, ES CORRECTA" : "Ô£à S├ì, ES CORRECTO";
+    } else if (age >= 13 && age < 18) {
+        // Adolescente
+        confirmLabel = "Ô£à S├ì, SOY YO";
+    } else if (age >= 18 && age < 65) {
+        // Adulto
+        confirmLabel = isFemale ? "Ô£à S├ì, SOY USUARIA" : "Ô£à S├ì, SOY USUARIO";
+    } else if (age >= 65) {
+        // Adulto Mayor
+        confirmLabel = isFemale ? "Ô£à S├ì, ES CORRECTA" : "Ô£à S├ì, ES CORRECTO";
+    }
+
+    return {
+        confirmLabel,
+        rejectLabel: "ÔØî NO, CORREGIR DATOS"
+    };
+};
diff --git a/diff.txt b/diff.txt
index 8049cf1..caeff3c 100644
Binary files a/diff.txt and b/diff.txt differ
diff --git a/diff_utf8.txt b/diff_utf8.txt
index e231755..7289749 100644
Binary files a/diff_utf8.txt and b/diff_utf8.txt differ
diff --git a/server/routes/cortexRoutes.js b/server/routes/cortexRoutes.js
index abacc83..986d353 100644
--- a/server/routes/cortexRoutes.js
+++ b/server/routes/cortexRoutes.js
@@ -66,7 +66,7 @@ Analiza este caso y devuelve el JSON correspondiente.
 `;
 
         const response = await ai.models.generateContent({
-            model: 'gemini-1.5-flash',
+            model: 'gemini-2.5-flash',
             contents: prompt,
             config: {
                 systemInstruction: SYSTEM_PROMPT,
@@ -98,10 +98,11 @@ Analiza este caso y devuelve el JSON correspondiente.
         res.json(jsonResponse);
 
     } catch (error) {
-        console.error("­ƒöÑ Error en /api/cortex/analyzeMotive:", error.message);
+        console.error("­ƒöÑ Error en /api/cortex/analyzeMotive:", error);
         res.status(500).json({
             success: false,
-            message: "Error de comunicaci├│n con GEM Semantic Intelligence."
+            message: "Error de comunicaci├│n con GEM Semantic Intelligence.",
+            details: error.message
         });
     }
 });
